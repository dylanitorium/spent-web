import React from 'react';
import styles from './styles.scss';

const WelcomeTitle = () => (
  <div className={styles.container}>
    <h1 className={styles.title}>Spent.</h1>
  </div>
);

export default WelcomeTitle;
